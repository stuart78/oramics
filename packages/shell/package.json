{
  "name": "@oramics/shell",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell: window management, projector display, file save",
  "author": "Stuart Smith",
  "main": "main.cjs",
  "scripts": {
    "dev": "node dev.mjs",
    "icon": "node ../../scripts/make-icon.mjs",
    "start": "electron .",
    "stage": "pnpm --filter @oramics/app run build && node stage.mjs",
    "build": "pnpm run stage && electron-builder --config electron-builder.config.js --mac --publish never",
    "build:unsigned": "CSC_IDENTITY_AUTO_DISCOVERY=false pnpm run stage && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.config.js --mac --publish never",
    "build:dir": "CSC_IDENTITY_AUTO_DISCOVERY=false pnpm run stage && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.config.js --mac --dir --publish never",
    "release": "pnpm run stage && electron-builder --config electron-builder.config.js --publish always"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8"
  }
}
